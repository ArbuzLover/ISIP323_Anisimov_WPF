<Page x:Class="ISIP323_Anisimov_WPF.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ISIP323_Anisimov_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage" Background="LightCyan">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
           <TextBox Grid.Row="0" Width="400" x:Name="Search" Height="25" HorizontalAlignment="Left" Margin="10,0,0,0" TextChanged="Search_TextChanged" />
            
            <Button Content="Сортировка по рейтингу"  Grid.Row="0"  Margin="10" Click="Button_Click"  />
            <Button Content="Сортировка по названию"  Grid.Row="0"  Margin="10" Click="Button_Click_1" />
        </StackPanel>
        <ListBox x:Name="FilmsListBox" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="1" SelectionChanged="FilmsListBox_SelectionChanged">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                   
                    <StackPanel>
                        <Image  Width="300" Height="300" Source="{Binding ImagePath}"/>

                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center"/>
                        <TextBlock  Text="{Binding RateFilm}" HorizontalAlignment="Center"/>
                        <TextBlock  Text="{Binding StartDate}" HorizontalAlignment="Center"/>
                        <TextBlock  Text="{Binding RateAge.Age}" HorizontalAlignment="Center"/>
                    </StackPanel>
                    
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Page>
