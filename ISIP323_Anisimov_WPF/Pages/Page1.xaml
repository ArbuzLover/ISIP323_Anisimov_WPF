<Page x:Class="ISIP323_Anisimov_WPF.Pages.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ISIP323_Anisimov_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1"  Background="#FFA9A8A8">

    <Grid>
 
        <Grid.RowDefinitions>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>


        <ListBox x:Name="ProductListBox" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                    <Image  Width="300" Height="300" Source="{Binding Path}"/>

                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center"/>
                        <TextBlock  Text="{Binding Price}" HorizontalAlignment="Center"/>
                            <Button Content="Добавить" x:Name="Product1ButtonAdd" Click="Product1ButtonAdd_Click" Grid.Row="1"/>
                        
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="20">
            <Button Content="Перейти к заказу" x:Name="Page2Button" Click="Page2Button_Click"/>
        </StackPanel>
        
        
    </Grid>
</Page>
