<Window x:Class="ISIP323_Anisimov_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ISIP323_Anisimov_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel  HorizontalAlignment="Center" Orientation="Horizontal">
            <Label Content="ХЗ ДЛЯ ТЕКСТА" x:Name="TitleLabel" />
            <Button Content="Вернуться назад" x:Name="BackButton" Click="BackButton_OnClick" Width="100" Height="20" />
        </StackPanel>
        <Frame x:Name="MainFrame" Grid.Row="1"  Source="Pages/Step1.xaml" NavigationUIVisibility="Hidden" Navigated="MainFrame_OnNavigated"/>
    </Grid>
</Window>
